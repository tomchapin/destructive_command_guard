name: telemetry-e2e

on:
  pull_request:
    paths:
      - "src/telemetry/**"
      - "tests/common/**"
      - "tests/telemetry_*.rs"
      - "tests/e2e/run_telemetry_e2e.sh"
      - ".github/workflows/telemetry-e2e.yml"
  push:
    branches: [master, main]
    paths:
      - "src/telemetry/**"
      - "tests/common/**"
      - "tests/telemetry_*.rs"
      - "tests/e2e/run_telemetry_e2e.sh"
      - ".github/workflows/telemetry-e2e.yml"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@nightly

      - name: Build release
        run: cargo build --release

      - name: Run telemetry E2E
        run: |
          chmod +x tests/e2e/run_telemetry_e2e.sh
          tests/e2e/run_telemetry_e2e.sh 2>&1 | tee telemetry-e2e.log

      - name: Upload logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: telemetry-e2e-logs
          path: telemetry-e2e.log
